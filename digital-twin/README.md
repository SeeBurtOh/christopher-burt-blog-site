# Copy your LinkedIn PDF export here before building:
#   digital-twin/cb_me/linkedin.pdf
#
# Update knowledge index (local):
#   OPENAI_API_KEY=sk-... npm run build:knowledge
#
# Netlify runs build:knowledge on deploy if OPENAI_API_KEY is set in site env vars.
# Alternatively commit netlify/functions/knowledge.json after building locally.
